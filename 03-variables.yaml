- name: Global variables
  hosts: local
  connection: local
  vars:
   COURSE: ansible
   DURATION: 10 hrs
   TRAINER: Siva
  tasks:
  - name: Global Variables
    ansible.builtin.debug:
     msg: "This is {{ COURSE }} session length is {{ DURATION }} & trainer is {{ TRAINER }}"

    
